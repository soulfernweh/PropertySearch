# Domain API — get a key from https://developer.domain.com.au
DOMAIN_API_KEY=your_domain_api_key_here

# Google Maps — enable Geocoding API + Routes API in Google Cloud Console
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Server port (optional, defaults to 3000)
PORT=3000
